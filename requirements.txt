# BiasClean — pinned dependencies (Phase 4, Workstream D)
# All bounds are exact (==) rather than >= to prevent silent drift on a
# fresh install months from now. Versions below are the ones confirmed
# working in the original venv (via `python3 -m pip freeze`), NOT
# whatever a >= bound would resolve to today — a fresh install with
# the old >= bounds during this validation pass already resolved
# reportlab to an untested 5.0.0 and svglib to an untested 2.1.0
# before being pinned back down, which is exactly the drift this
# workstream exists to prevent.

pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4
seaborn==0.13.2
scikit-learn==1.6.1
fairlearn==0.13.0
reportlab==4.5.1
xhtml2pdf==0.2.17
python-bidi==0.6.11
pypdf==6.14.2
Pillow==11.3.0
html5lib==1.1
pyHanko==0.33.0
pyhanko-certvalidator==0.29.1
arabic-reshaper==3.0.0
svglib==2.0.2
